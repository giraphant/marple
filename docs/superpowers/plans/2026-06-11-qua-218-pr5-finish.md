# QUA-218 PR5 — 收尾：清警告 + 架构梳理（按当前文件目录）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** QUA-218 终期收尾，**只做两件真价值的事**：① 清掉基线构建警告（干净构建）；② 把 PR1-4 迁移后的真实架构，按当前文件目录树梳理成一份准确的 `ARCHITECTURE.md`。

**不做（已与用户确认）：** AppModel 拆 extension、Integrations 挪文件、删转发——这些是装修不是架构，纯文件重排对 LLM 可读性无实益、对耦合无改善。QUA-218 的架构优化已由 PR1-4 达成（单 MarpleKit 核心 + Catalog 收口）；PR5 不挖更多"架构"。

**Architecture:** 两个独立小任务。警告清理是点状 build-config/注解/缓存修复（行为零变化）。架构文档是只读勘察 + 重写 `ARCHITECTURE.md` 反映现实（不动代码）。

**Tech Stack:** Swift 6 / SwiftPM。`apple/.build` 符号链接**永不删**。基线警告（本期清零）：CollectionGridVariant MainActor ×2、VectorStore cblas_sgemv ×1、Fixtures unhandled ×1。`swift test` 收尾可能 SIGPIPE exit 13（QUA-208）。

---

### Task 1: 清四类基线警告 → 干净构建

**Files:** `Package.swift`、`apple/Sources/Marple/Browse/CollectionGridVariant.swift`、`SupersetLog.swift`、DateFormatter 两处 zh_CN 站点。

- [ ] **Step 1: 基线警告快照**

```bash
cd apple && touch Sources/Marple/Browse/CollectionGridVariant.swift Sources/MarpleKit/Vault/VectorStore.swift && swift build 2>&1 | grep -B1 -A3 "warning:" | head -40
```
记录确切文本：CollectionGridVariant dataSource/delegate MainActor ×2、VectorStore cblas_sgemv 弃用 ×1、Fixtures unhandled ×1。

- [ ] **Step 2: Fixtures resource（Package.swift）**

`.testTarget(name: "MarpleKitTests", ...)` 加 `resources: [.process("Fixtures/qwen3-embedding-0.6B-oracle.json")]`。先 grep 加载该 fixture 的测试用 `Bundle.module` 还是硬路径——若硬路径，用 `.copy` 或 `exclude:` 保测试绿。报告所选 + 加载测试仍绿。

- [ ] **Step 3: VectorStore CBLAS 旗（Package.swift）**

MarpleKit target `swiftSettings` 加 `.unsafeFlags(["-DACCELERATE_NEW_LAPACK", "-DACCELERATE_LAPACK_ILP64"])`。VectorStore.swift 的 cblas_sgemv 调用**不改**。若 ILP64 旗致 Int32 类型不匹配编译错，只留 `-DACCELERATE_NEW_LAPACK`。报告最终旗 + 警告消失 + 向量搜索测试绿。

- [ ] **Step 4: CollectionGridVariant MainActor**

读 `beginPreviewPanelControl`（~L351）的 `panel.dataSource = self`/`panel.delegate = self`。QLPreviewPanelController 协议方法 nonisolated、dataSource/delegate 是 @MainActor 属性。用 `MainActor.assumeIsolated { ... }` 包两行赋值（Quick Look 本就主线程调），或 override 标 `@MainActor`——读真实签名定方案。验证两警告消失、预览不变。

- [ ] **Step 5: DateFormatter 缓存/收拢**

`SupersetLog.swift:43` 每行 `ISO8601DateFormatter()` → `private static let`（输出逐字不变）。两处 zh_CN `"yyyy-MM-dd HH:mm"`（SettingsView:463、Backup/BackupBrowserView:132）收拢到 `apple/Sources/Marple/Shared/DateFormatters.swift` 的一个共享 static——**仅当两处格式串+locale 完全一致才合并**，否则只缓存不合并、报告差异。GitDates 已 static 的 ISO8601 不动。

- [ ] **Step 6: 警告清零 + 测试 + Commit**

```bash
cd apple && touch Sources/Marple/Browse/CollectionGridVariant.swift Sources/MarpleKit/Vault/VectorStore.swift && swift build 2>&1 | grep -c "warning:"   # 期望 0
cd apple && swift test 2>&1 | tail -3   # 全绿
git add -A apple/Sources Package.swift
git commit -m "chore(cleanup): silence baseline warnings (MainActor/CBLAS/Fixtures) + cache DateFormatters (QUA-218)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 架构梳理 — 重写 ARCHITECTURE.md 反映当前文件树

**目的**：把 PR1-4 迁移终态的真实架构，按当前 `apple/Sources/MarpleKit/`、`apple/Sources/Marple/`、`apple/ios/` 目录树，梳理成一份准确、可从文件树读懂分层的 `ARCHITECTURE.md`。**只读勘察 + 写文档，不动任何代码。**

**Files:** Modify `apple/ARCHITECTURE.md`（92 行，停在 PR2，缺 Session/ 完整 Catalog/ iOS 终态）。

- [ ] **Step 1: 勘察当前真实结构**

读现 `apple/ARCHITECTURE.md`。然后逐目录勘察（`ls` + 抽读关键文件首部注释/公开类型），列出每个目录的职责 + 关键类型：
- **MarpleKit 核心**（共享，平台无关）：`Model/`（Entry/EntryType…）、`Schema/`（L0 声明表 VaultSchema）、`Vault/`（L1 VaultClient/LocalVaultClient/IndexDatabase/VectorStore/FrontmatterPatch/VaultWatcher/VaultChangeSource）、`Indexer/`（写侧 VaultIndexer/IndexWriter/YamlFrontmatter/SourceResolver）、`Catalog/`（L2 Catalog + 扩展 + NameResolver/RelationGraph/Containers/RefreshAuthority/SearchRanker/ThemeIndex/TopicIndex/Browse/BookContext/TopicContext/RelationsIndex/DocStats/DocOutline）、`Markdown/`（L3 MarkdownModel/Wikilink）、`Nav/`（L4 Navigation/PersistedState/SessionSnapshot）、`Session/`（L4 SessionWriter/MetadataWriter/SessionResolver）、`Localisation/`、`CLI/`、`Backup/`。逐个确认真实内容（grep 类名），不要照抄旧文档。
- **Marple 壳（macOS）**：`App/`（AppModel 组合根 + MarpleApp）、`Browse/`、`Reading/`、`Inspector/`、`Sidebar/`、`Tabs/`、`Settings/`、`Shared/`、`CLI/`(socket 宿主)、`Backup/`(UI+scheduler)、`Resources/`。
- **iOS 壳**：`App/`（ReaderModel + SetupView）、`UI/`（DocScreen/SidebarScreen/EntryListScreen/MarkdownTextView/RootView）、`Vault/`（IOSVaultClient/VaultBookmark/ICloudMaterializer）。

- [ ] **Step 2: 重写 ARCHITECTURE.md**

结构建议（按文档现有中文风格）：
1. **一句话定位** + 分层模型 L0 Schema → L1 Vault → L2 Catalog → L3 Document(Markdown) → L4 Session/Nav（保留现有分层叙述，更新到现状）。
2. **三棵树**：MarpleKit 核心 / Marple 壳 / iOS 壳——各目录一行职责 + 关键类型（用真实勘察结果）。标出"单核心 + 两薄壳"：两壳都消费同一 MarpleKit，壳里只剩平台 UI + 边界胶水（FSEvents vs iCloud、NSOpenPanel vs fileImporter、socket、security-scope）。
3. **依赖方向**：Model 最底；Vault/Indexer/Catalog/Markdown/Nav/Session/Schema 只往下依赖 Model，横向基本不互引（保留 Schema→Indexer YAML 的有名字例外）。
4. **关键架构事实**（QUA-218 终态）：Catalog 是 L2 派生状态唯一 owner + 统一 generation/单飞（RefreshAuthority + per-pass `pass`，derivedGeneration 独立轴）；NameResolver 两级匹配统一；规则①②引擎 + 点名例外（annotationAnchor/PDF Jaccard）；VaultChangeSource 契约（watcher/iCloud 共同实现）；写回原语（SessionWriter/MetadataWriter）下沉、iOS 只读是产品选择。
5. **过渡期留壳的诚实标注**：entries + loadIndex body 仍在两壳（Catalog 持派生与权威，orchestration body 过渡留壳）；字段语义 + persist 编排留壳。这样文档不撒谎。

更新现有 92 行里过时处（如缺 Session/、Catalog 类型表补全、iOS 段）。可附一个 ASCII 目录树速览。

- [ ] **Step 3: 自检 + Commit**

通读：每个真实目录都在文档里？依赖方向准确？没有已删的旧名（如 Derivation/）？没把过渡留壳说成已完成？
```bash
git add apple/ARCHITECTURE.md
git commit -m "docs(architecture): rewrite ARCHITECTURE.md to current post-QUA-218 file tree (QUA-218)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 终验 + PR

- [ ] **Step 1: 全量 + 警告清零**

```bash
cd apple && touch Sources/Marple/Browse/CollectionGridVariant.swift Sources/MarpleKit/Vault/VectorStore.swift && swift build 2>&1 | grep -c "warning:"   # 0
cd apple && swift test 2>&1 | tail -3   # 全绿
```

- [ ] **Step 2: iOS 构建**

```bash
cd apple/ios && xcodegen generate && xcodebuild build -project MarpleiOS.xcodeproj -scheme MarpleiOS -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/marple-ios-dd CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3
```
BUILD SUCCEEDED（Package.swift 的 ACCELERATE 旗 + Fixtures resource 不影响 iOS）。

- [ ] **Step 3: push + PR**

```bash
git push -u origin qua-218-pr5
gh pr create --title "chore: QUA-218 收尾 — 清警告 + 架构梳理 (PR5)" --body "<见下>"
```
PR body：① 清零基线警告（MainActor/CBLAS/Fixtures/DateFormatter 缓存），干净构建；② 重写 ARCHITECTURE.md 反映 PR1-4 迁移终态的真实文件树（单核心+两薄壳、L0-L4 分层、依赖方向、Catalog 收口事实、过渡留壳的诚实标注）。**明确不做** AppModel 拆分/Integrations 挪文件/删转发——与用户确认为装修非架构，QUA-218 架构优化已由 PR1-4 达成。行为零变化；测试全绿，iOS 构建过。标注 QUA-218 收尾完成。

**注：** 本 PR 无 GUI 行为改动（清警告等价 + 文档），可跳 make install GUI 抽查;若想稳妥,装一下验 Quick Look 预览(CollectionGridVariant 改点)+ 语义搜索(CBLAS 改点)即可。

---

## Self-Review 记录

- **范围诚实**：只做清警告（真价值）+ 架构文档（用户要的"梳理"）。拆分/Integrations/删转发已与用户确认丢弃（装修）。
- **零变化**：警告修等价（CBLAS 同调用 / @MainActor 不改主线程运行时 / Fixtures 构建声明 / DateFormatter 缓存不改输出）；架构文档不动代码。
- **文档诚实**：Task 2 Step 2 第 5 点要求标注过渡留壳，不把未完成说成完成。
