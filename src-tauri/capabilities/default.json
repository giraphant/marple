{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults for the main window. Custom app commands are allowed once registered in invoke_handler.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
