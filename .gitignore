node_modules
dist
.vite
*.log
.DS_Store

# Local launcher config
.superset/

# Native build artifacts (regenerable via apple/build-app.sh)
apple/Artwork/
apple/Marple.app/

# iOS app: generated by xcodegen from apple/Mobile/project.yml (run `xcodegen generate`)
apple/Mobile/*.xcodeproj

# Rust build output (legacy)
legacy/rust/target/

# Local machine config (legacy)
legacy/marple.config.json

# iOS release secrets (key id/issuer; .p8 lives in ~/.appstoreconnect)
apple/Mobile/.release.env
